package com.example;

import static org.junit.Assert.assertEquals;
import static com.example.Ejercicio1.*;

import org.junit.Test;

/*
Pruebas Unitarias para el Ejercicio 1

La funcionalidad que se esta implementando es una funcion que determina si un numero es primo o no, el alcance de esta función es esencial en diversos contextos matemáticos y computacionales donde se requiere identificar números primos, como en algoritmos de criptografía, teoría de números, y ciertos algoritmos de optimización. 
 */
public class Ejercicio1Test {
    
    /*
    @Test
    public void Test_NumeroPrimo() {
        // caso prueba 1
        // Se espera que el numero 2 sea primo

        
        int numeroObtenido = 2;
        boolean expected = true;
        numeroObtenido = esPrimo(numeroObtenido);

        assertEquals(expected, numeroObtenido);
    }

    @Test
    public void Test_NumeroPrimo2() {
        // caso prueba 2
        // Se espera que el numero 1 no sea primo

        int numeroObtenido = 1;
        boolean expected = false;

        numeroObtenido = esPrimo(numeroObtenido);

        assertEquals(expected, numeroObtenido);
    }

    @Test
    public void Test_NumeroPrimo3() {
        // caso prueba 3
        // se espera que el numero -5 no sea primo

        int numeroObtenido = -5;
        boolean expected = false;

        numeroObtenido = esPrimo(numeroObtenido);

        assertEquals(expected, numeroObtenido);
    }

    @Test
    public void Test_NumeroPrimo4() {
        // caso prueba 4
        // se espera que el numero sea null y caiga en la excepcion
        
    }

    */
}
